namespace Smartstore.Core.Content.Posts
{
    /// <summary>
    /// Specifies the number of columns to display in a post list layout.
    /// </summary>
    /// <remarks>This enumeration is typically used to configure the layout of a post list,  allowing the
    /// caller to specify whether the list should be displayed in two or three columns.</remarks>
    public enum PostListColumns
    {
        Two = 2,
        Three = 3
    }
}
