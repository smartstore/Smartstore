@for (var i = 1; i <= 3; i++)
{
    var googleFont = Display.GetThemeVariable<string>("google-font-" + i);
    if (googleFont.HasValue())
    {
        <link href='https://fonts.googleapis.com/css?family=@googleFont' rel='stylesheet' type='text/css' />
    }
}