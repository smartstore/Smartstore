@model CommentListModel

@if (Model.Comments.Count == 0)
{
    return;
}

<div id="customer-commentlist" class="block commentlist mb-5">
    <h4 id="customer-commentlist-title" class="block-title post-commentlist-title">
        <span>@T("News.Comments")</span>
        <small class="text-muted lrm">(@Model.NumberOfComments)</small>
    </h4>

    <div class="block-body p-0" role="list" aria-labelledby="customer-commentlist-title">
        @foreach (var comment in Model.Comments)
        {
            <partial name="Comment.Item" model="comment" />
        }
    </div>
</div>