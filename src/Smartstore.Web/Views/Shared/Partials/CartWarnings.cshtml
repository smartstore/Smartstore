@model List<string>

<div sm-if="Model.Count > 0" class="alert alert-danger" role="alert">
    <ul>
        @foreach (var warning in Model)
        {
            <li>@Html.Raw(warning)</li>
        }
    </ul>
</div>
