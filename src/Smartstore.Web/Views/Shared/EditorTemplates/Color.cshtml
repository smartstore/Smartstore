@functions{
    private string Value
    {
        get
        {
            string value = null;
            if (ViewData.Model != null)
            {
                value = Convert.ToString(ViewData.Model);
            }
            return value;
        }
    }
}

<colorbox asp-for="@Model"
          id="@Html.IdForModel()"
          name="@Html.NameForModel()" 
          value="@Value.EmptyNull()" 
          sm-default-color="@(ViewData["fallbackColor"].Convert<string>())" />