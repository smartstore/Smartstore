@model Smartstore.Admin.Models.Common.CurrencyModel

@{
    ViewBag.Title = T("Admin.Configuration.Currencies.EditCurrencyDetails").Value;
}

<form asp-action="Edit">
    <div class="section-header">
        <div class="title">
            <a sm-backto asp-action="List"></a>
            <i class="far fa-money-bill-alt"></i>
            <span>@ViewBag.Title - @Model.Name</span>
        </div>
        <div class="options">
            <button type="submit" name="save" value="save" class="btn btn-warning">
                <i class="fa fa-check"></i>
                <span>@T("Admin.Common.Save")</span>
            </button>
            <button type="submit" name="save" value="save-continue" class="btn btn-secondary">
                <span>@T("Admin.Common.SaveContinue")</span>
            </button>
            <button type="submit" id="currency-delete" name="currency-delete" class="btn btn-danger">
                <i class="far fa-trash-alt"></i>
                <span>@T("Admin.Common.Delete")</span>
            </button>
            <confirm button-id="currency-delete" />
        </div>
    </div>

    <partial name="_CreateOrUpdate" model="Model" />
</form>