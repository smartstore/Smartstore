@using Smartstore.Core.Checkout.Payment
@using Smartstore.Core.Checkout.Shipping

@model OrderListModel

@{
    var stores = ViewBag.Stores as IList<SelectListItem>;
}

<div class="form-group">
    <div class="form-row">
        <div class="col">
            <smart-label asp-for="StartDate" />
            <editor asp-for="StartDate" />
        </div>
        <div class="col">
            <smart-label asp-for="EndDate" />
            <editor asp-for="EndDate" />
        </div>
    </div>
</div>
<div class="form-group">
    <smart-label asp-for="CustomerName" />
    <input asp-for="CustomerName" />
</div>
<div class="form-group">
    <smart-label asp-for="CustomerEmail" />
    <input asp-for="CustomerEmail" />
</div>
<div class="form-group">
    <smart-label asp-for="OrderGuid" />
    <input asp-for="OrderGuid" />
</div>
<div class="form-group">
    <smart-label asp-for="OrderNumber" />
    <input asp-for="OrderNumber" />
</div>
<div class="form-group">
    <smart-label asp-for="PaymentMethods" />
    <select asp-for="PaymentMethods" asp-items="ViewBag.PaymentMethods" multiple="multiple"></select>
</div>
<div sm-if="stores.Count > 1" class="form-group">
    <smart-label asp-for="StoreId" />
    <select asp-for="StoreId" asp-items="stores" asp-placeholder="@T("Admin.Common.All")"></select>
</div>
<div class="form-group">
    <smart-label asp-for="OrderStatusIds" />
    <select asp-for="OrderStatusIds" asp-items="Html.GetLocalizedEnumSelectList(typeof(OrderStatus))" multiple="multiple"></select>
</div>
<div class="form-group">
    <smart-label asp-for="PaymentStatusIds" />
    <select asp-for="PaymentStatusIds" asp-items="Html.GetLocalizedEnumSelectList(typeof(PaymentStatus))" multiple="multiple"></select>
</div>
<div class="form-group">
    <smart-label asp-for="ShippingStatusIds" />
    <select asp-for="ShippingStatusIds" asp-items="Html.GetLocalizedEnumSelectList(typeof(ShippingStatus))" multiple="multiple"></select>
</div>
