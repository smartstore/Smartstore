@model IRuleExpression

<div class="rule-descriptor font-weight-medium form-control-plaintext text-truncate text-gray" title="@(Model.Descriptor.Description.NullEmpty() ?? Model.Descriptor.DisplayName) (@(Model.Descriptor.Name))">
    @Model.Descriptor.DisplayName
</div>