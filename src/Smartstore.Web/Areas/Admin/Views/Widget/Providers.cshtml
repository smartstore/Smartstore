@using Smartstore.Admin.Models.Modularity

@model IEnumerable<WidgetModel>

@{
    ViewBag.Title = T("Admin.ContentManagement.Widgets").Value;
}

<div class="section-header">
    <div class="title">
        <i class="fa fa-plus-square"></i>
        @T("Admin.ContentManagement.Widgets")
    </div>
</div>

<div class="provider-list">
    @(await Html.ProviderList(Model))
</div>
