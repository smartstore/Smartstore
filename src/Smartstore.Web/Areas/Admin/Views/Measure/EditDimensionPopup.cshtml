@using Smartstore.Admin.Models.Common

@model MeasureDimensionModel

@{
    ViewBag.Title = T("Admin.Configuration.Measures.Dimensions.EditDimension").Value;
    Layout = "_AdminPopupLayout";
}

<form asp-action="EditDimensionPopup" asp-route-btnId="@ViewBag.BtnId" asp-route-formId="@ViewBag.FormId">
    <div class="section-header">
        <div class="title">
            @T("Admin.Configuration.Measures.Dimensions.EditDimension")
        </div>
        <div class="options">
            <button type="submit" name="save" id="save" value="save" class="btn btn-warning">
                <i class="fa fa-check"></i>
                <span>@T("Admin.Common.Save")</span>
            </button>
        </div>
    </div>

    <partial name="_CreateOrUpdateDimension" model="Model" />
</form>