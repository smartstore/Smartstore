@using Smartstore.Admin.Models.Common

@model QuantityUnitModel

@{
    ViewBag.Title = T("Admin.Configuration.QuantityUnit.AddNew").Value;
    Layout = "_AdminPopupLayout";
}

<form asp-action="CreateQuantityUnitPopup" asp-route-btnId="@ViewBag.BtnId" asp-route-formId="@ViewBag.FormId">
    <div class="section-header">
        <div class="title">
            @T("Admin.Configuration.QuantityUnit.AddNew")
        </div>
        <div class="options">
            <button type="submit" name="save" value="save" class="btn btn-primary">
                <i class="fa fa-check"></i>
                <span>@T("Admin.Common.Save")</span>
            </button>
        </div>
    </div>

    <partial name="_CreateOrUpdate" model="Model" />
</form>