@using Smartstore.Admin.Models.Orders

@model CheckoutAttributeValueModel

@{
    ViewBag.Title = T("Admin.Catalog.Attributes.CheckoutAttributes.Values.AddNew").Value;
    Layout = "_AdminPopupLayout";
}

<form asp-action="ValueCreatePopup" id="checkout-attribute-values" asp-route-btnId="@ViewBag.BtnId" asp-route-formId="@ViewBag.FormId">
    <div class="section-header">
        <div class="title">
            @T("Admin.Catalog.Attributes.CheckoutAttributes.Values.AddNew")
        </div>
        <div class="options">
            <button type="submit" name="save" value="save" class="btn btn-primary">
                <i class="fa fa-check"></i>
                <span>@T("Admin.Common.Save")</span>
            </button>
        </div>
    </div>

    <partial name="_CreateOrUpdateValue" model="Model" />
</form>