@using Smartstore.Admin.Models.Orders

@model CheckoutAttributeModel

@{
    ViewBag.Title = T("Admin.Catalog.Attributes.CheckoutAttributes.AddNew").Value;
}

<form asp-action="Create" id="checkoutattribute-form">
    <div class="section-header">
        <div class="title">
            @T("Admin.Catalog.Attributes.CheckoutAttributes.AddNew")
            <a asp-action="List">(@T("Admin.Catalog.Attributes.CheckoutAttributes.BackToList"))</a>
        </div>
        <div class="options">
            <button type="submit" name="save" value="save" class="btn btn-primary">
                <i class="fa fa-check"></i>
                <span>@T("Admin.Common.Save")</span>
            </button>
            <button type="submit" name="save" value="save-continue" class="btn btn-secondary">
                <span>@T("Admin.Common.SaveContinue")</span>
            </button>
        </div>
    </div>

    <partial name="_CreateOrUpdate" model="Model" />
</form>