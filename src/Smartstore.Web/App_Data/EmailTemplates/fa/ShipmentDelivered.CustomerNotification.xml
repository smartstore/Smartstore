<?xml version="1.0" encoding="utf-8"?>
<MessageTemplate>
	<To>{{ Customer.FullName }} &lt;{{ Customer.Email }}&gt;</To>
    <Subject>{{ Store.Name }}. محموله شما تحویل داده شده است.</Subject>
    <ModelTypes>Shipment, Shipment.Order, Shipment.Customer</ModelTypes>
    <Body>
        <![CDATA[{% extends 'master' %}

{% block 'body' %}
<h1>
  سلام {{ Customer.FullName }}،
</h1>

<p>
  محموله شما با شناسه <b>{{ Shipment.Id }}</b> تحویل داده شده است.
</p>

<table class="w-100 mb-3">
  <thead>
  	<tr>
      <th class="text-muted text-sm">تاریخ سفارش</th>
      <th class="text-muted text-sm">شماره سفارش</th>
      <th class="text-muted text-sm">شناسه ردیابی</th>
      <th class="text-muted text-sm">تاریخ ارسال</th>
    </tr>
  </thead>
  <tbody class="mb-3">
  	<tr>
      <td class="text-sm">{{ Order.CreatedOn | Date:'d' }}</td>
      <td class="text-sm">{{ Order.OrderNumber }}</td>
      <td class="text-sm">{{ Shipment.TrackingNumber }}</td>
      <td class="text-sm">{{ Shipment.ShippedOn | Date:'d' }}</td>
    </tr>
  </tbody>
</table>

{% if Order.Url %}
<p class="mt-4 mb-4 text-center">
  <a class="btn btn-primary" href="{{ Order.Url }}">مشاهده جزئیات</a>
</p>
{% endif %}

<table class="w-100 brd">
  <tbody>
  	<tr>
    	<td class="p-2">
        
        <table class="order-details w-100">
          <tbody>
            <tr>
              <td width="50%">
                <h3>آدرس صورت‌حساب</h3>
              	{% include 'address' with Order.Billing %}
                {% if Order.PaymentMethod %}
                	<h3 class="mt-2">روش پرداخت</h3>
                	<div class="text-sm">{{ Order.PaymentMethod }}</div>
                {% endif %}
              </td>
              <td width="50%">
                <h3>آدرس ارسال</h3>
                {% if Order.Shipping %}
					{% include 'address' with Order.Shipping %}
                {% else %}
                	<p class="text-muted text-sm">مشابه آدرس صورت‌حساب</p>
                {% endif %} 
                {% if Order.ShippingMethod %}
                	<h3 class="mt-2">روش ارسال</h3>
                	<div class="text-sm">{{ Order.ShippingMethod }} </div>    	
                {% endif %}
              </td>
            </tr>
          </tbody>
        </table>
        
      </td>
    </tr>
  </tbody>
</table>
<p> </p>
{% include 'shipment' %}

{% endblock %}]]>
    </Body>
</MessageTemplate>