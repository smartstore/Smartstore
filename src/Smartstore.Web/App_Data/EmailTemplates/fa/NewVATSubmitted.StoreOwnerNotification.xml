<?xml version="1.0" encoding="utf-8"?>
<MessageTemplate>
	<To>{{ Email.DisplayName }} &lt;{{ Email.Email }}&gt;</To>
	<ReplyTo>{{ Customer.FullName }} &lt;{{ Customer.Email }}&gt;</ReplyTo>
    <Subject>{{ Store.Name }}. شماره مالیات بر ارزش افزوده جدید ثبت شد</Subject>
    <ModelTypes>Customer</ModelTypes>
    <Body>
        <![CDATA[{% extends 'master' %}

{% block 'body' %}
<h1>
  شماره مالیات بر ارزش افزوده جدید،
</h1>

<p>
  <b>{{ Customer.FullName }}</b> (<a href="mailto:{{ Customer.Email }}">{{ Customer.Email }}</a>) یک شماره مالیات بر ارزش افزوده جدید ثبت کرده است.
</p>

<p class="mt-4 mb-4 text-center">
  <a class="btn btn-primary" href="{{ Customer.EditUrl }}">مشاهده پروفایل مشتری</a>
</p>

{% if Customer.VatNumber %}
<p>
  <b>شماره مالیات بر ارزش افزوده:</b><br />
  <span>{{ Customer.VatNumber }}</span>
</p>
{% endif %}

{% if Customer.VatNumberStatus %}
<p>
  <b>وضعیت:</b><br />
  <span>{{ Customer.VatNumberStatus }}</span>
</p>
{% endif %}

<p>
  <b>دریافت شده توسط:</b><br />
  <span>{{ VatValidationResult.Name }} - {{ VatValidationResult.Address }}</span>
</p>

{% endblock %}]]>
    </Body>
</MessageTemplate>