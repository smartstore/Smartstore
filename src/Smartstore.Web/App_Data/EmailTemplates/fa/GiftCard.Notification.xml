<?xml version="1.0" encoding="utf-8"?>
<MessageTemplate>
	<To>{{ GiftCard.RecipientName }} &lt;{{ GiftCard.RecipientEmail }}&gt;</To>
	<ReplyTo>{{ GiftCard.RecipientName }} &lt;{{ GiftCard.RecipientEmail }}&gt;</ReplyTo>
    <Subject>{{ GiftCard.SenderName }} برای شما یک کارت هدیه از {{ Store.Name }} ارسال کرده است</Subject>
    <ModelTypes>GiftCard, GiftCard.PurchasedWithOrderItem, GiftCard.PurchasedWithOrderItem.Product, GiftCard.PurchasedWithOrderItem.Order.Customer</ModelTypes>
    <Body>
        <![CDATA[{% extends 'master' %}

{% block 'body' %}
<h1>
  سلام {{ GiftCard.RecipientName }}،
</h1>

<p>
  شما یک کارت هدیه به مبلغ <b class="text-success text-lg">{{ GiftCard.Amount }}</b> برای فروشگاه آنلاین <a href="{{ Store.Url }}">{{ Store.Name }}</a> دریافت کرده‌اید.
</p>

<table class="w-100">
	<tr>
  	<td class="bg-success text-white p-3 text-center">
        <b>کد کارت هدیه شما:</b><br />
  		<b class="text-xl">{{ GiftCard.CouponCode }}</b>
    </td>
  </tr> 
</table>

<p> </p>

<table class="w-100 brd bg-light">
	<tr>
  	<td class="p-3">
      <h3 class="mb-3">
        پیام شخصی
      </h3>
      <p>
        {{ GiftCard.Message }}
      </p>
    </td>
  </tr> 
</table>

{% endblock %}]]>
    </Body>
</MessageTemplate>