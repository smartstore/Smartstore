@using Smartstore.Twitter.Auth.Models

@model ConfigurationModel

@{
    Layout = "_ConfigureProvider";
}

<div class="alert alert-info alert-dismissible">
    @Html.Raw(T("Plugins.ExternalAuth.Twitter.AdminInstruction"))
    <button type="button" class="btn-close" data-dismiss="alert"></button>
</div>

@await Component.InvokeAsync("StoreScope")

<form asp-action="Configure">
    <widget target-zone="admin_button_toolbar_before">
        <button id="SaveConfigButton" type="submit" name="save" class="btn btn-warning" value="save">
            <i class="fa fa-check"></i>
            <span>@T("Admin.Common.Save")</span>
        </button>
    </widget>

    <div asp-validation-summary="All"></div>

    <div class="adminContent">
        <div class="adminRow">
            <div class="adminTitle">
                <smart-label asp-for="ConsumerKey" />
            </div>
            <div class="adminData">
                <setting-editor asp-for="ConsumerKey"></setting-editor>
                <span asp-validation-for="ConsumerKey"></span>
            </div>
        </div>
        <div class="adminRow">
            <div class="adminTitle">
                <smart-label asp-for="ConsumerSecret" />
            </div>
            <div class="adminData">
                <setting-editor asp-for="ConsumerSecret"></setting-editor>
                <span asp-validation-for="ConsumerSecret"></span>
            </div>
        </div>
        <div class="adminRow">
            <div class="adminTitle">
                <smart-label asp-for="RedirectUrl" />
            </div>
            <div class="adminData wide">
                <input asp-for="RedirectUrl" class="form-control-plaintext" readonly />
            </div>
        </div>
    </div>
</form>