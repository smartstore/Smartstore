@using Smartstore.Core.Content.Menus

@model (LinkResolutionResult, string, string)

@{ 
    var label = Model.Item2;
    var value = Model.Item3;
}

<select class="form-control transferable resettable"
        data-select-url="@Url.Action("AllNews", "NewsAdmin", new { area = "Admin" })"
        data-select-selected-id="@value"
        data-select-init-text="@label"></select>
